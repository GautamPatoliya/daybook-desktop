# App icons for electron-builder

- `icon.png` — **1024×1024** source (macOS requires ≥512×512; Windows/macOS packaging)
- Optional overrides: `icon.ico` (Windows), `icon.icns` (macOS)

Replace with branded artwork before public/signed releases. Do not use icons smaller than 512×512.
