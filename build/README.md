# App icons for electron-builder

- `icon.png` — 256×256+ source (used for Windows/macOS packaging)
- Optional overrides: `icon.ico` (Windows), `icon.icns` (macOS)

Replace with branded artwork before public/signed releases.
